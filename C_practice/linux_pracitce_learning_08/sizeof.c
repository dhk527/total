#include <stdio.h>

int main(){
	int a;

	int b;
	char c;
	double d;

	
	printf("a의 값 %d size : %ld\n", a, sizeof(a));
	printf("b의 값 %d size : %ld\n", b, sizeof(b));
	printf("c의 값 %c size : %ld\n", c, sizeof(c));
	printf("d의 값 %f size : %ld\n", d, sizeof(d));
}
// 너네 bit가 32 64bit 인지 아닌지를 확인하려고 왜냐면 16bit int 2byte 등이 뭔지 확인하려고 ㅇㅇ

			
